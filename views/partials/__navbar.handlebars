<nav class="navbar bg-dark border-bottom border-body" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Produtos App</a>
    {{#if user}}
       <span class="navbar-text text-white">Bem-vindo, {{username}}!</span>
    {{else}}
    {{/if}}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        
        {{#if user}}  <!-- Verifica se o usuário está autenticado -->
          <li class="nav-item">
            <a class="nav-link" href="/produtos">Produtos</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/usuario/login/sair">Logout</a>
          </li>
          
          {{#if user.eadmin}}  <!-- Verifica se o usuário é administrador -->
            <li class="nav-item">
              <a class="nav-link" href="/admin/adicionarprodutos">Adicionar produtos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/admin/editProdutos">Gerenciar produtos</a>
            </li>
          {{/if}}
        {{else}}  <!-- Caso contrário, exibe links de Login e Registro -->
          <li class="nav-item">
            <a class="nav-link" href="/usuario/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/usuario/registro">Registro</a>
          </li>
        {{/if}}
      </ul>
      <form class="d-flex" role="search" method="get" action="/produtos/search">
        <input class="form-control me-2" type="search" name="searchTerm" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>